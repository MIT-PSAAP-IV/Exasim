# Documentation site toolchain (MkDocs + Material).
# Build:   pip install -r requirements-docs.txt && mkdocs build --strict
# Serve:   mkdocs serve
mkdocs==1.6.1
mkdocs-material==9.7.6
pymdown-extensions==10.21.3
